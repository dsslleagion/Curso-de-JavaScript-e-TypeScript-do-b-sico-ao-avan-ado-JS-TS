// tipos de dados primitivos 

// string, number, undefined, null, boolean

// Strings
const nome = 'Dionisio';
const nome1 = "Dionisio";
const nome2 = `Dionisio`; // crase utiizado para templates strings

// number
const num1 = 10; // inteiro
const num2 = 10.52; // flutuante

// inicialização de variavel 
let nomeAluno; // sem declarar nada na variavel ela se torna undefined
// variavel que não aponta a lugar nenhum na memoria 

const sobrenomeAluno = null; // valor Nulo -> não aponta a lugar nenhum na memoria 

// diferença de null e undefined eu utilizo explicitamente eu quero que essa variavel não tenha não aponte a lugar nenhum 
// agora o undefined nunca utiliza o valor

// tipos de dados 
// tipo de dado boolean true ou false (valor logico)
const boolean = true;

const aprovado = true | false;

// console.log(typeof nome, nome); // typeof exibe o tipo do dado

// Qual é a diferença de um tipo de dado primitivo e  porque ?

// tipo de dado primitivo X tipo de dado passado por referencia
// = sinal de atribuição 
// const a = [1, 2];
// const b = a;
// console.log(a, b);

// b.push(3);
// console.log(a, b);

// quando se cria uma variavel por referencia quer dizer que a gente criou 
// uma variavel 

// let a = 2;
// let b = a; //variavel de referencia 
// console.log(a, b); // 2, 2

// a = 3;
// console.log(a, b); // 3, 2



